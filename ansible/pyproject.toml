[project]
name = "braunicore-ansible"
version = "0.1.0"
description = "Secret-safe Ansible control project for the Braunicore fleet"
requires-python = ">=3.12"
dependencies = [
  "ansible-core==2.21.3",
]

[tool.uv]
package = false
